# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Backup & temp files（*.bak* 通配覆盖 .bak10 等多位后缀）
*.bak*
*.orig
InkWord_Firmware/1

# Qoder IDE 计划/会话文件
.qoder/

# IDE / clangd 生成物
compile_commands.json
InkWord_Firmware/compile_commands.json
*.d

# macOS Finder metadata
Info/

# Firmware diagnostics logs
/tmp/inkword*.log

# 默认词库原始数据（tools/default_vocab/.raw/，含第三方完整数据文件，
# 不入库；由 download.py 重新下载，生成产物 out/ 与 SeedData/ 才入库）
tools/default_vocab/.raw/

# 默认词库读音文件（第三方真人音下载产物，fetch_audio.py 可重新生成；
# CSV/JSON 的 audio 引用随之重建，不入库）
tools/default_vocab/out/audio_raw/
tools/default_vocab/out/audio/
# TF 卡旧词库快照（临时回滚档）
tools/default_vocab/out/tf_backup_*/

# 后端运行数据（不入库）：sherpa-onnx ASR 模型（689M 第三方大文件，
# asr-models releases 下载部署，见 docs/AI_CHAT_MODE.md §7.3）与
# TTS 音频运行产物
InkWord_Backend/src/InkWord.API/data/
